<h5>Details</h5>
<table border="1" cellpadding="5" cellspacing="1">
  <tr>
    <th>Player</th>
    <td><%= @player.full_name %></td>
  </tr>
  <tr>
    <th>Email</th>
    <td><%= @player.email %></td>
  </tr>
  <tr>
    <th>Phone</th>
    <td><%= @player.phone %></td>
  </tr>
</table>
<p/>
<h5>Games Played</h5>
<% if @player.games.empty? %>
<i>None</i>
<% else %>
<table border="1" cellpadding="5" cellspacing="1">
  <tr>
    <th>Match</th>
    <th>Game</th>
    <th>Place</th>
  </tr>
<%   for result in @player.results %>
  <tr>
    <td><%= link_to "Match ##{result.game.match.id} on #{result.game.match.match_date}", match_path(result.game.match) %></td>
    <td><%= link_to "#{result.game.name}", game_path(result.game.id) %></td>
    <td><%= result.place %></td>
  </tr>
<%   end %>
</table>
<% end %>
<p/>
