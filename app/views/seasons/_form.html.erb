<%= form_for(@season) do |f| %>
  <% if @season.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@season.errors.count, "error") %> prohibited this season from being saved:</h2>

      <ul>
      <% @season.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <table>
    <tr>
      <th><%= f.label :ordinal %></th>
      <td><%= f.text_field :ordinal %></td>
    </tr>
    <tr>
      <th><%= f.label :start_year %></th>
      <td><%= f.text_field :start_year %></td>
    </tr>
    <tr>
      <th><%= f.label :stop_year %></th>
      <td><%= f.text_field :stop_year %></td>
    </tr>
  </table>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
