language: ruby
rvm:
  - 2.5.8
services:
  - postgresql

addons:
  postgresql: "12"
  apt:
    packages:
    - postgresql-12
    - postgresql-client-12

before_script:
  - cp config/database.yml.travis config/database.yml
  - "psql -c 'create database sppl_travis_test;' -U postgres"
  - "bundle exec rake db:migrate"

script: bundle exec rake test
